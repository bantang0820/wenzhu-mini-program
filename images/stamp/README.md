# 印章资源说明

## 🎨 印章设计

当前使用代码绘制的临时印章：
- 位置：右下角
- 颜色：中国红 (#C8102E)
- 内容："稳"字
- 样式：圆形，双圈，稍微倾斜

## 📌 优化建议

### 方式一：使用真实印章图片（推荐）

如果你有设计师朋友，可以让他们设计一个真实的印章图片：

**设计要求**：
- 格式：PNG（透明背景）
- 尺寸：200x200px 或更大
- 样式：传统印章，稍微做旧
- 颜色：中国红 (#C8102E)
- 内容："稳"字 或 "安"字

**使用方法**：
1. 将图片放到 `/images/stamp/` 目录
2. 修改 `card.js` 中的 `drawStamp` 方法：
```javascript
// 使用图片替代代码绘制
const stampImage = canvas.createImage();
stampImage.src = '/images/stamp/stamp.png';
stampImage.onload = () => {
  ctx.drawImage(stampImage, x, y, size, size);
};
```

### 方式二：在线生成印章

使用在线工具生成印章：
- 搜索"印章生成器"或"公章制作"
- 输入"稳"字
- 选择圆形样式
- 下载PNG格式

### 方式三：继续使用代码绘制（当前方案）

优点：
- 无需额外资源
- 可以自由调整颜色、大小、角度
- 加载速度快

缺点：
- 缺少真实印章的质感
- 没有做旧效果

## 🔧 当前实现

在 `pages/card/card.js` 的 `drawStamp` 方法中：

```javascript
drawStamp(ctx, x, y, size) {
  ctx.save();
  ctx.translate(x, y);
  ctx.rotate(-5 * Math.PI / 180); // 稍微倾斜

  // 印章边框（圆形）
  ctx.strokeStyle = '#C8102E';
  ctx.lineWidth = 3;
  ctx.beginPath();
  ctx.arc(0, 0, size / 2, 0, Math.PI * 2);
  ctx.stroke();

  // 内圈
  ctx.strokeStyle = '#C8102E';
  ctx.lineWidth = 1;
  ctx.beginPath();
  ctx.arc(0, 0, size / 2 - 8, 0, Math.PI * 2);
  ctx.stroke();

  // "稳"字
  ctx.fillStyle = '#C8102E';
  ctx.font = 'bold 36px Songti SC, serif';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText('稳', 0, 0);

  ctx.restore();
}
```

## 💡 效果优化

如果想让印章更真实，可以添加：
1. **噪点效果**：模拟印泥的不均匀
2. **缺角效果**：模拟印章的使用痕迹
3. **渐变透明**：模拟印油的深浅变化

## 📝 建议

**MVP阶段**：使用代码绘制即可
**正式上线**：考虑使用专业设计的印章图片
